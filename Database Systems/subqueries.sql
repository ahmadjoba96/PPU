-- Course Database Systems - PPU - Dr. Mahmoud Al-Saheb
-- BY:A7mad_Joba
-- Environment: Oracle Live SQL


SELECT ENAME, SAL
FROM EMP
WHERE SAL > (
  SELECT AVG(SAL) FROM EMP
);


SELECT ENAME, DEPTNO
FROM EMP
WHERE DEPTNO = (
  SELECT DEPTNO FROM EMP WHERE ENAME = 'Ahmad'
);


SELECT *
FROM DEPT
WHERE DEPTNO IN (
  SELECT DISTINCT DEPTNO FROM EMP
);


SELECT ENAME, SAL
FROM EMP
WHERE SAL = (
  SELECT MAX(SAL) FROM EMP
);